---
phase: 05-cli-visualization
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-05]

must_haves:
  truths:
    - "User confirms TUI dashboard displays correctly during live MIDI performance"
    - "User confirms --quiet mode preserves previous behavior"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete CLI visualization experience — live MIDI performance with TUI dashboard and --quiet fallback.

Purpose: Confirms the TUI meets design intent (performance dashboard feel, not debugger) and works with real MIDI hardware.
Output: Verification pass/fail with any issues noted.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cli-visualization/05-CONTEXT.md
@.planning/phases/05-cli-visualization/05-01-SUMMARY.md
@.planning/phases/05-cli-visualization/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete CLI visualization with real-time TUI dashboard showing stack state, operation history, and program output during live MIDI performance. Includes --quiet fallback to preserve Phase 4 scrolling log behavior.
  </what-built>
  <how-to-verify>
**Test 1: TUI mode (default)**
1. Connect a MIDI device
2. Run: `cargo run -p haydn --` (no --quiet flag)
3. Verify: Three-panel TUI appears with "haydn" title, Rounded borders
4. Play notes on MIDI device
5. Verify: Stack panel updates with values (shows ASCII chars for printable range like `72 'H'`)
6. Verify: Operations panel scrolls with each note event
7. Verify: Output panel accumulates text when PrintChar/PrintNum operations fire
8. Verify: Status bar shows tuning name, device name, loop state
9. Press 'q' to quit — terminal restores cleanly, session summary prints

**Test 2: --quiet mode**
1. Run: `cargo run -p haydn -- --quiet`
2. Verify: No TUI — same scrolling text log as Phase 4
3. Play a few notes, confirm format matches: `[NoteName v=vel] → Mapping  |  Stack: [...]`
4. Ctrl+C to quit, confirm session summary prints

**Test 3: Visual personality check (per D-01, D-02)**
- Does it feel like a "performance dashboard" (clean, glanceable) rather than a dense debugger?
- Does it look like a sibling of haydn-performer's TUI? (Similar border style, density, personality)
- Is the output panel visually distinct as the "money moment" for Hello World?
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
Human confirms all three test scenarios pass.
</verification>

<success_criteria>
1. TUI renders correctly on user's terminal
2. Real-time updates are perceptibly immediate during MIDI performance
3. --quiet mode matches Phase 4 behavior exactly
4. Visual design meets performer dashboard intent
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-visualization/05-03-SUMMARY.md`
</output>
