---
phase: 09-distribution-release
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - README.md
  - docs/esolang-wiki.md
autonomous: false
requirements: [DIST-01, DIST-02]

must_haves:
  truths:
    - "README contains a deadpan academic Acknowledgments section referencing LLM assistance"
    - "Esolang wiki page contains the same acknowledgments"
    - "Satirical academic framing is consistent across both documents"
    - "cargo install haydn link is live before wiki submission"
  artifacts:
    - path: "README.md"
      provides: "Updated README with Acknowledgments section"
      contains: "Acknowledgment"
    - path: "docs/esolang-wiki.md"
      provides: "Updated wiki page with Acknowledgments section"
      contains: "Acknowledgment"
  key_links:
    - from: "README.md"
      to: "crates.io"
      via: "cargo install haydn link"
      pattern: "cargo install haydn"
---

<objective>
Add deadpan academic LLM acknowledgments to README and esolang wiki page (per D-14), then create a checkpoint for wiki submission after cargo install link is live (per D-10). Preserves the satirical academic paper framing throughout (per D-11).

Purpose: LLM disclosure per project decision. Wiki submission completes the release checklist. Both maintain the project's distinctive voice.
Output: Updated README.md, updated docs/esolang-wiki.md, wiki submission verified.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-distribution-release/09-CONTEXT.md

<interfaces>
<!-- Current README structure — executor needs to know section order -->

README.md sections (in order):
```
# Haydn
## Abstract
## 1. Introduction
## 2. Quick Start
## 3. Methods (Instruction Set)
## 4. Results (Example Programs)
## 5. Discussion
## References
## License
```

docs/esolang-wiki.md sections (in order):
```
# Haydn
## Overview
## Computational Model
## Instruction Set
## Event Interface
## Control Flow
## Hello World
## Computational Class
## Tuning System
## External Resources
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LLM acknowledgments to README and wiki page</name>
  <files>README.md, docs/esolang-wiki.md</files>
  <action>
    Per D-14: Add a deadpan academic "Acknowledgments" section to both documents. Generic reference to "large language models" — no specific model names, no scoping of what was vs. wasn't LLM-assisted. Fits naturally into the satirical academic paper framing. Tone: dry, formal, absurd context — matching the rest of the documents.

    **README.md:** Insert an `## Acknowledgments` section between `## References` and `## License`. The section should read as a genuine academic acknowledgments section — thanking LLMs in the same breath one might thank a research assistant or a particularly reliable compiler. Keep it 2-4 sentences. Do NOT name specific models. Do NOT enumerate what was/wasn't LLM-assisted.

    **docs/esolang-wiki.md:** Insert an `## Acknowledgments` section before `## External Resources`. Same tone, can be shorter (1-2 sentences). Wiki pages are terser.

    Per D-11: The satirical academic framing must be preserved. These sections should sound like they belong in an actual academic paper's acknowledgments — the humor is in the context (thanking an LLM for help with an esolang), not in the wording.
  </action>
  <verify>
    <automated>cd c:\Users\jared\source\repos\haydn; Select-String -Path "README.md","docs/esolang-wiki.md" -Pattern "Acknowledgment" | Measure-Object | Select-Object -ExpandProperty Count</automated>
    Count must be >= 2 (at least one match in each file). Verify no specific model names appear:
    Select-String -Path "README.md","docs/esolang-wiki.md" -Pattern "GPT|Claude|Gemini|Copilot|ChatGPT|Llama" | Measure-Object | Select-Object -ExpandProperty Count
    Must be 0.
  </verify>
  <done>Both documents have Acknowledgments sections. No specific model names. Academic tone is consistent with surrounding content.</done>
</task>

<task type="auto">
  <name>Task 2: Compile release checklist</name>
  <files>docs/RELEASE-CHECKLIST.md</files>
  <action>
    Create `docs/RELEASE-CHECKLIST.md` — a concise checklist for the v0.0.1 release. This is a one-time operational reference, not ongoing documentation. Include:

    **Pre-release:**
    - [ ] All tests pass locally (`cargo test --workspace`)
    - [ ] `cargo publish --dry-run` passes for all 5 crates
    - [ ] `CARGO_REGISTRY_TOKEN` configured as GitHub repo secret
    - [ ] Repository URLs consistent in all Cargo.toml files

    **Release:**
    - [ ] Create and push git tag: `git tag v0.0.1 && git push origin v0.0.1`
    - [ ] Wait for CI to pass on the tag (GitHub Actions)
    - [ ] Review draft GitHub Release (auto-created by CI)
    - [ ] Click "Publish release" to trigger crate publishing
    - [ ] Verify all 5 crates appear on crates.io
    - [ ] Test: `cargo install haydn` works from a clean environment

    **Post-release:**
    - [ ] Submit esolang wiki page (copy from docs/esolang-wiki.md)
    - [ ] Verify `cargo install haydn` link in wiki page works

    Keep it short — just the checklist, no prose.
  </action>
  <verify>
    <automated>cd c:\Users\jared\source\repos\haydn; Test-Path "docs/RELEASE-CHECKLIST.md"; Get-Content "docs/RELEASE-CHECKLIST.md" | Select-String "\[ \]" | Measure-Object | Select-Object -ExpandProperty Count</automated>
    File exists and contains checklist items (count >= 10).
  </verify>
  <done>Release checklist exists as a runnable operational reference for the v0.0.1 release.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final release review</name>
  <files>README.md, docs/esolang-wiki.md, .github/workflows/ci.yml, .github/workflows/release.yml, docs/RELEASE-CHECKLIST.md</files>
  <action>
    Present the user with a summary of all Phase 9 artifacts for approval:
    - All 5 Cargo.toml files audited and ready for crates.io (Plan 01)
    - GitHub Actions CI and release workflows created (Plan 02)
    - LLM acknowledgments added to README and wiki page
    - Release checklist compiled

    The crates are NOT yet published — that requires the user to:
    1. Configure `CARGO_REGISTRY_TOKEN` as a GitHub repo secret
    2. Push changes to GitHub (triggers CI)
    3. Follow the release checklist to tag and publish

    Verification steps for the user:
    1. Review the Acknowledgments sections in README.md and docs/esolang-wiki.md — confirm tone matches the satirical academic framing
    2. Review .github/workflows/ci.yml — confirm 3 OS matrix makes sense
    3. Review .github/workflows/release.yml — confirm draft approval model is correct
    4. Run `cargo test --workspace` — confirm nothing is broken
    5. Run `cargo publish --dry-run -p haydn-vm` — confirm publish readiness
    6. Review docs/RELEASE-CHECKLIST.md — confirm steps are complete
  </action>
  <verify>
    <automated>cd c:\Users\jared\source\repos\haydn; cargo test --workspace 2>&1 | Select-String "test result" | Select-Object -Last 1</automated>
  </verify>
  <done>User approves all Phase 9 artifacts. Release preparation is complete and ready for the user to execute the release checklist.</done>
</task>

</tasks>

<verification>
- Acknowledgments sections present in both README.md and docs/esolang-wiki.md
- No specific LLM model names appear anywhere in acknowledgments
- Academic tone is consistent with surrounding satirical framing
- Release checklist covers full workflow from pre-release through wiki submission
- All Phase 9 artifacts are ready for the user to execute the release
</verification>

<success_criteria>
Release preparation is complete. User has a clear checklist to follow for the v0.0.1 release. LLM acknowledgments are in place. Wiki submission is part of the post-release checklist. DIST-01 through DIST-05 are satisfied (DIST-01/02 via CI + publish readiness; DIST-03/04/05 verified as already implemented).
</success_criteria>

<output>
After completion, create `.planning/phases/09-distribution-release/09-03-SUMMARY.md`
</output>
